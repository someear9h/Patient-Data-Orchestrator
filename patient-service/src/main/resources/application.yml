spring:
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 5000

billing:
  transport: grpc # switch to "rest" for the REST run

audit:
  mode: async

logging:
  level:
    root: WARN
    com.pm: WARN